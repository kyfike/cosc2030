	.data
str1:	.asciz	"%d"
	.align	2
str2:	.asciz	"You entered %d\n"
	.align	2
n:	.word	0

	.text
	.globl main
main:	stmfd	sp!, {lr}

	ldr	r0, =str1
	ldr	r1, =n
	bl	scanf

	ldr	r0, =str2
	ldr	r1, =n
	ldr	r1, [r1]
	bl	printf

	ldmfd	sp!, {lr}
	mov	r0, #0
	mov	pc, lr
	.end
